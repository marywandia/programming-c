// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int age;
    printf("Enter the age of the person");
    scanf("%d", &age);
    if(age>=18);
    {
    printf("u r  very eligible");
    }
   if(age<=18);
    {
        printf("u r  eligible");
    } 
    return 0;
}
    